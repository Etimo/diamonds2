import webpack = require('webpack');
import { Configuration } from '../configuration';
import { PluginsLoader } from './plugins-loader';
export declare class WebpackCompiler {
    private readonly pluginsLoader;
    constructor(pluginsLoader: PluginsLoader);
    run(configuration: Required<Configuration>, webpackConfigFactoryOrConfig: (config: webpack.Configuration) => webpack.Configuration, tsConfigPath: string, appName: string, isDebugEnabled?: boolean, watchMode?: boolean, onSuccess?: () => void): void;
}
