export type FireGameObjectProperties = {
  takeDiamonds: boolean;
};

export type FireProviderConfig = {
  /**
   * The number of pairs of teleporters that will be generated.
   */
  fire: number;
};
