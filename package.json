{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cross-env MY_ENV_VAR=postgresql://diamonds:password@database:5433/diamonds?schema=public (cd packages/backend && npx prisma generate) && npx tsc --build packages/backend packages/frontend --verbose",
    "env": "./scripts/copy-env.sh",
    "init-db": "npm run support && ./scripts/setup-db-prisma.sh",
    "nodemon": "npx nodemon",
    "start:backend": "cd packages/backend && npm run start:dev",
    "start:frontend": "cd packages/frontend && npm run dev",
    "start": "npm run support && concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run nodemon\"",
    "support": "docker-compose up -d database",
    "postinstall": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.7.1",
    "@commitlint/config-conventional": "^17.7.0",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "husky": "^9.0.11",
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
